# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# --- Testing / coverage ---
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# --- Type checkers ---
.mypy_cache/
.dmypy.json
dmypy.json
.pyre/
.pytype/

# --- Environments ---
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# --- VS Code ---
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# --- Conda ---
*.conda
.conda/
conda-meta/

# --- Jupyter ---
.ipynb_checkpoints

# --- OS ---
.DS_Store
Thumbs.db

# --- Project-specific data / outputs ---
# Structures and derived files are fetched/regenerated by scripts/, but the
# ensemble manifests are curated inputs (PDB ID -> ligand residue name) that
# cannot be regenerated from the fetch scripts, and the README's reproduction
# steps take them as arguments, so they are tracked.
data/*
!data/.gitkeep
!data/*/
data/*/*
!data/*/*manifest*.json
# ...but not the prepped manifests, which prep_ensemble.py generates and
# which point at local .sdf/.pkl paths that do not exist in a fresh clone.
data/*/*prepped*.json
*.log
*.pdb
*.sdf
*.mol2
*.pdbqt
*.pkl
outputs/
results/
plip_out/
results*.csv
failures.csv
